import React from "react";

import { Box } from "@mui/system";

function App() {
  return (
    <Box
      className="App"
      sx={{ backgroundColor: "red", width: "100%", minHeight: "100vw" }}
    >
      <h1>React App</h1>
    </Box>
  );
}

export default App;
